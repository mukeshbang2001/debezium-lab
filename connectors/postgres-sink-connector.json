{
  "name": "pg-sink",
  "config": {
    "connector.class": "io.debezium.connector.jdbc.JdbcSinkConnector",
    "tasks.max": "1",
    "topics": "mongo.shop.customers",
    "connection.url": "jdbc:postgresql://postgres:5432/auditdb",
    "connection.user": "test",
    "connection.password": "test",
    "insert.mode": "insert",
    "auto.create": "false",
    "delete.enabled": "false",
    "table.name.format": "mongo_audit",
    "value.converter.schemas.enable": "false"
  }
}

